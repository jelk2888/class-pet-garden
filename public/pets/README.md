# 宠物图片目录

完整等级图（含 `my-*` 九级图鉴）体积较大，**不纳入 Git 仓库**。

## 获取方式

1. **本地开发**：将图片放到本目录，结构为 `{petId}/lv0.png` … `lv8.png`
2. **群晖 / 完整部署包**：使用目录 `班级宠物系统_群晖WebStation/public/pets/`（含全量图）
3. **从「我的宠物」四阶段生成九级图**：
   ```bash
   python scripts/build_my_pets_9levels.py
   ```

缺少图片时，系统仍可运行，但图鉴与学生卡片可能显示裂图。
